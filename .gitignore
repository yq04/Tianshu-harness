node_modules/
dist/
out/
*.tsbuildinfo

# TypeScript emitted files alongside sources (tsup outputs to dist/)
src/**/*.js
src/**/*.d.ts
src/**/*.js.map
src/**/*.d.ts.map
.DS_Store
failure-samples/**/transcript.raw.jsonl

# Session artifacts
findings.md
progress.md
task_plan.md
.superpowers/
docs/sessions/

# Local project reference materials
其他项目资料/

# Local agent runtime state
.claude/worktrees/
.claude/skills/**/*.bak
.claude/*.lock
.claude/PRPs/reviews/
.wolf/
.omc/
.github/workflows/
# 一次性/本地诊断脚本与评估产物
desktop/scripts/perf-*.ts
# 例外：perf-session-open-script.test.ts 会 spawn 这个脚本，它已经不是一次性
# 诊断而是可复现基线。被 ignore 挡在库外时那条测试只在作者本机能过，任何全新
# clone 都挂 ERR_MODULE_NOT_FOUND。（同类的 perf-stream-baseline.ts 早已入库——
# ignore 规则对已跟踪文件不生效，不需要额外例外。）
!desktop/scripts/perf-session-open.ts
scripts/summarize-*.ts
scripts/swebench-*.sh
# 自动生成的计划、研究与设计草稿
# (已移除 .rivet/plans/*.md 和 docs/superpowers/specs/*.md 的 ignore——
#  这些是 agent 的设计知识资产，gitignore 会导致 read_file 工具拒绝读取，
#  迫使 agent 用 bash 绕路。源码层面已同步修复 read-file.ts 的豁免逻辑。)
docs/research/*.md
.rivet/sensorium.jsonl
.rivet/pheromones.json
.rivet/debug-400.json
.rivet/artifacts/
.rivet/sessions/
.rivet/cache-log.jsonl
.rivet/cache/
.rivet/benchmark/
.rivet/skills/deep-brainstorm/
# brainstorming 为 bundled 技能（runtime-assets/bundled-skills/）的 seed 种入副本，勿入库
.rivet/skills/brainstorming/
desktop/.rivet/
.cc-switch/
.rivet/check2.txt
.rivet/*.txt
.rivet/tauri-key/
.rivet/frames.jsonl
.rivet/tool-stream.jsonl
# dsh-meridian.db*：2026-09-08 旧命名实验的残留库（现役为 meridian.db*）
.rivet/meridian.db*
.rivet/dsh-meridian.db*
.rivet/playbook.jsonl
.rivet/knowledge/memory.jsonl
.rivet/knowledge/commit-facts.jsonl
.rivet/knowledge/advisory-efficacy.jsonl
.rivet/generals/*.md
.gitlog_temp.txt
.rivet/runtime/
.rivet/tmp/
.rivet/external/
.rivet/prefix-diag.jsonl
.rivet/tool-stream-*.jsonl
.rivet/test-results.json
.rivet/checkpoints/
.rivet/skills/_drafts/
.rivet/Tianshu/
.rivet/plans/draft-*.md

.zcode/

# AI agent temp files
.tmp/
.tmp_*
截图/
.omx/
.tmp-iso-*
.tmp-abort-*
.tmp-test-fixture
.test-tmp/

# 退役捆绑技能的本地备份（writing/executing-plans 消融 + 撤出三项——
# 纯本地归档，不进提交、不进打包；git 历史与 docs/skills/optional/ 仍保有内容）
skills-backup/

# === Agent / tool 运行态（生成物，非源码）===
# .rivet: 策展子目录(knowledge/skills/plans/agents/...)保持跟踪，仅忽略纯运行态
.rivet/backups/
.rivet/vsw/
.rivet/semantic-index.json
.rivet/recovery-journal.jsonl
.rivet/presence.json
.rivet/constellation.json
# 其它无策展内容的工具目录，整目录忽略
# .cursor/ 目录下 plans 文件为重要设计文档，需保留；只忽略 Cursor 的 settings/rules/workspace-state
.cursor/settings.json
.cursor/rules
.cursor/mcp.json
.cursor/workspace-state
.cursor/tasks
.od-skills/
# 误产生的空文件
/js
# npm pack 产物
*.tgz

# Release build artifacts（保留 latest.json 作为 manifest 继续跟踪）
# 打包产物目录整体忽略（曾只忽略 dmg/tar.gz/sig，AppImage 漏网导致 release/ 出现在 git status）
release/

# 日志与评估产物
logs/
tianshu-agent-v1.*.json

# 运行时插件 / scratch 目录
.rivet/plugin-*/
.rivet/scratch/
# 本地 launcher / 项目级覆盖配置（勿入仓）
.rivet/config.json

# 品牌源素材草稿（成品图标走 desktop/src-tauri/icons；勿把 brand-src 打进发版树）
desktop/brand-src/

# macOS 本地应用数据
Library/

# 一次性临时产物 / 导出的文档产物
_test-docx.html
docs/*.html
docs/*.docx

# 冒烟/评测产物（不发布；sync 脚本只同步指定目录，天然不进公开仓）
todo-cli/

# starflow 编排器运行态（状态文件随会话变化，不入仓）
.rivet/starflow/

# 第三方运行时数据目录（WorkBuddy 等工具的本地 memory）
# 第三方运行时数据目录（WorkBuddy 等工具的本地 memory）
.workbuddy/
# dsh 系工具的本地记忆数据（dsh-memory 格式，非本项目资产）
.dsh/

# 测试临时目录（测试中断时可能残留，不应进入 git status）
src/**/__tests__/.scoped-commit-tmp/
src/**/__tests__/.undo-test-backup/

# 密钥与本地环境（安全：env 值即使写入 .env 也永不进 git；secrets.json 由 secrets-store 管理）
.env
.env.*
!.env.example

# 运行时索引（本地生成，非源码——对齐 main 2026-09-02 历史清理条目）
.rivet/knowledge/memory-index.sqlite

# 公开仓：禁止 knowledge 回灌（私有仓策略不同；仅保留 .rivet/SELF）
.rivet/knowledge/

# MATLAB TheBestColor 原包（运行时用 plugins/tianshu-research/figure/，不入库 .p / RAR）
*TheBestColor.rar

# Tauri bundling staging
desktop/src-tauri/resources/plugins-staged/
# Local research workspace data
.rivet/research/
tianshu_article.txt